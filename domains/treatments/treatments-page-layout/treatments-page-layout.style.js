import styled from "styled-components";

export const TreatmentsPageWrapper = styled.div`
  background-color: ${({ theme }) => theme.colors.canvas.primaryLight};
  min-height: 100vh;
  padding: ${({ theme }) => theme.spacing.six} 0;
`;
