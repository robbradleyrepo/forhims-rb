import styled from "styled-components";

import { ListResetStyles } from "../../elements/";

export const CartLineItemListStyle = styled.ul`
  ${ListResetStyles};
`;

CartLineItemListStyle.displayName = "CartLineItemListStyle";
