import styled from "styled-components";

export const VisitHeaderContainer = styled.div`
  display: flex;
  flex-direction: column;
  background-color: ${({ theme }) => theme.colors.emr.headingBg};
  margin: auto;
  width: 100%;
`;
